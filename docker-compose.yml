services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: talky
      POSTGRES_PASSWORD: talky_dev
      POSTGRES_DB: talky
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
